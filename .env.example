# Environment variables for local development (no secrets)
# Copy to .env and set values as needed

# Default AWS region (required)
AWS_REGION=us-east-1

# AWS CLI profile name (optional; prefer SSO-based profiles)
# AWS_PROFILE=default

# If present in your environment, MUST equal AWS_REGION to avoid ambiguity
# AWS_DEFAULT_REGION=us-east-1
