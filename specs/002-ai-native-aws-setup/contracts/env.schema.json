{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://example.com/env.schema.json",
  "title": "Environment Configuration Schema",
  "type": "object",
  "properties": {
    "AWS_REGION": {
      "type": "string",
      "description": "Default AWS region for development and smoke tests.",
      "default": "us-east-1",
  "pattern": "^[a-z]{2}-[a-z]+-\\d$"
    },
    "AWS_DEFAULT_REGION": {
      "type": "string",
      "description": "Optional. If set, should match AWS_REGION to avoid ambiguity.",
  "pattern": "^[a-z]{2}-[a-z]+-\\d$"
    },
    "AWS_PROFILE": {
      "type": "string",
      "description": "Optional. AWS CLI profile name (prefer SSO-based profiles)."
    }
  },
  "required": [
    "AWS_REGION"
  ]
}
